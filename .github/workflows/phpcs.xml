<?xml version="1.0"?>
<ruleset name="Custom Standard">
    <rule ref="PSR2">
        <!-- "PSR2" の中で除外するルール -->
        <exclude name="Generic.Files.LineLength"/>
    </rule>
    <!-- 追加するルール -->
    <!-- 除外するファイル・ディレクトリ -->
</ruleset>